<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
	Title="PSGUI"
    Height="1000"
	Width="1300"
    Style="{DynamicResource MaterialDesignWindow}"
	WindowStartupLocation="CenterScreen"
	ResizeMode="CanResize"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextElement.FontFamily="Calibri"
	TextOptions.TextFormattingMode="Ideal"
	TextOptions.TextRenderingMode="Auto"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <md:BundledTheme BaseTheme="Dark" PrimaryColor="Blue" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Window">
                <Style.Triggers>
                    <Trigger Property="IsActive" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                    </Trigger>
                    <Trigger Property="IsActive" Value="False">
                        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.InactiveBorderBrushKey}}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#FF1A1A1A"/>
                <Setter Property="BorderThickness" Value="5"/> 
                <Setter Property="CornerRadius" Value="8"/>
            </Style>

            <Style TargetType="Grid" x:Key="Command">
                <Setter Property="Background" Value="#FF252526"/>
            </Style>

            <Style TargetType="StackPanel">
                <Setter Property="Background" Value="#FF1A1A1A"/>
            </Style>

            <Style TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                <Setter Property="AlternatingRowBackground" Value="LightBlue"/>
            </Style>

            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="FontSize" Value="14"/>
            </Style>
            
            <Style TargetType="TabItem">
                <Setter Property="Background" Value="LightBlue"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TabItem}">
                            <Grid>
                                <Border Name="Border" Background="#FF303030"  CornerRadius="8,8,0,0" TextElement.FontSize="14" MinWidth="50">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="12,2,12,2"/>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True" >
                                    <Setter Property="Background" TargetName="Border" Value="#FF9E9E9E" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True" >
                                    <Setter Property="Background" TargetName="Border" Value="#FF9E9E9E" />
                                    <Setter Property="Cursor" TargetName="Border" Value="Hand" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True" >
                                    <Setter Property="Background" TargetName="Border" Value="#FF2096F4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="25" />
    </WindowChrome.WindowChrome>

    <Border>
        <Grid>
            <Grid Background="{DynamicResource MaterialDesignPaper}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Height="25" Orientation="Horizontal" FlowDirection="RightToLeft">
                    <Button Name="BtnClose" IsEnabled="True" Margin="0 0 4 4" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Exit" Style="{StaticResource MaterialDesignIconForegroundButton}" VerticalAlignment="Center" Height="25" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#9E9E9E">
                        <md:PackIcon Kind="Close" Height="20" Width="20"/>
                    </Button>
                    <Button Name="BtnMaximize" IsEnabled="True" Margin="0 0 4 4" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Maximize" Style="{StaticResource MaterialDesignIconForegroundButton}" VerticalAlignment="Center" Height="25" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#9E9E9E">
                        <md:PackIcon Kind="WindowMaximize" Height="20" Width="20"/>
                    </Button>
                    <Button Name="BtnMinimize" IsEnabled="True" Margin="0 0 0 4" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Minimize" Style="{StaticResource MaterialDesignIconForegroundButton}" VerticalAlignment="Center" Height="25" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#9E9E9E">
                        <md:PackIcon Kind="WindowMinimize" Height="20" Width="20"/>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="20 0 0 0" VerticalAlignment="Center" Orientation="Horizontal" Background="Transparent" HorizontalAlignment="Left">
                    <Button Name="BtnAdd" VerticalAlignment="Center" Style="{StaticResource MaterialDesignIconForegroundButton}">
                        <Button.LayoutTransform>
                            <ScaleTransform  ScaleX="0.95" ScaleY="0.95"/>
                        </Button.LayoutTransform>
                        <md:PackIcon Kind="AddBox" Foreground="LightGreen"/>
                    </Button>
                    <Button Name="BtnRemove" VerticalAlignment="Center" Style="{StaticResource MaterialDesignIconForegroundButton}">
                        <Button.LayoutTransform>
                            <ScaleTransform   ScaleX="0.95" ScaleY="0.95"/>
                        </Button.LayoutTransform>
                        <md:PackIcon Kind="TrashCan" Foreground="IndianRed"/>
                    </Button>
                    <Button Name="BtnSave" VerticalAlignment="Center" Style="{StaticResource MaterialDesignIconForegroundButton}">
                        <Button.LayoutTransform>
                            <ScaleTransform   ScaleX="0.95" ScaleY="0.95"/>
                        </Button.LayoutTransform>
                        <md:PackIcon Kind="Floppy" Foreground="CornflowerBlue"/>
                    </Button>
                    <ToggleButton Name="BtnEdit" VerticalAlignment="Center" Margin="5 0 0 0" Background="Transparent" Style="{StaticResource MaterialDesignActionToggleButton}">
                        <ToggleButton.LayoutTransform>
                            <ScaleTransform   ScaleX="0.95" ScaleY="0.95"/>
                        </ToggleButton.LayoutTransform>
                        <ToggleButton.Content>
                            <md:PackIcon Kind="Edit" Foreground="Orange"/>
                        </ToggleButton.Content>
                        <md:ToggleButtonAssist.OnContent >
                            <md:PackIcon Kind="EditOff" Foreground="White"/>
                        </md:ToggleButtonAssist.OnContent>
                    </ToggleButton>
                </StackPanel>

                <Grid Name="CommandDialog" Grid.Row="2" Panel.ZIndex="1000" Visibility="Hidden" VerticalAlignment="Center" HorizontalAlignment="Center" >
                    <Border>
                        <Grid Style ="{StaticResource Command}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Height="20" Orientation="Horizontal" Background="#FF1A1A1A" FlowDirection="RightToLeft">
                                <Button Name="BtnCommandClose" IsEnabled="True" Margin="0 0 4 4" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Exit" Style="{StaticResource MaterialDesignIconForegroundButton}" VerticalAlignment="Center" Height="25" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#9E9E9E">
                                    <md:PackIcon Kind="Close" Height="20" Width="20"/>
                                </Button>
                            </StackPanel>

                            <Grid Name="CommandGrid" Grid.Row="1" Style ="{StaticResource Command}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>
                                <!-- <Button Name="BtnCommonParameters" Content="Fold/Expand"/> -->
                            </Grid>
                            <Grid Grid.Row="2">
                                <Button Name="BtnCommandRun" Content="Run" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}" Width="300" Height="30" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Name="Main" Grid.Row="2">
                    <Border Margin="12 0 12 0">
                        <TabControl Name="TabControl" HorizontalContentAlignment="Left"/>
                    </Border>
                </Grid>

                <Button Name="BtnRun" Content="Run" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}" Grid.Row="3" Width="300" Height="30" HorizontalAlignment="Center" Margin="0,10,0,10" />
                <Grid Name="Spinner" Visibility="Hidden" Grid.Row="3" Margin="350 0 0 0" HorizontalAlignment="Center"  Height="30" Width="30">
                    <ProgressBar Foreground="White" Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True"/>
                </Grid>

                <md:Snackbar Name="Snackbar" Grid.Row="3" Height="70" Opacity="1" Margin="0 0 15 0" Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" MessageQueue="{Binding materialDesign:MessageQueue}" HorizontalAlignment="Right"/>

                <Grid Name="DebugGrid" Visibility="Visible" Grid.Row="3" Height="70" Width="480" HorizontalAlignment="Left">
                    <TextBox Name="DebugBox" BorderThickness="0,0,0,0"  VerticalScrollBarVisibility="Visible" TextWrapping="Wrap" />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>