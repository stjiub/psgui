<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    Style="{DynamicResource MaterialDesignWindow}"
	WindowStartupLocation="CenterScreen"
	ResizeMode="CanResize"
    Width="1200"
    Height="1000"
    MaxWidth="{x:Static SystemParameters.PrimaryScreenWidth}"
    MaxHeight="{x:Static SystemParameters.PrimaryScreenHeight}"
    Background="{DynamicResource MaterialDesignPaper}"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextElement.FontFamily="Calibri"
	TextOptions.TextFormattingMode="Ideal"
	TextOptions.TextRenderingMode="Auto"
    >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <md:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Purple" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="AppPrimaryBrush" Color="#2096F4"/>

            <Style TargetType="TabItem">
                <Setter Property="Background" Value="#2096F4"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TabItem}">
                            <Grid>
                                <Border Name="Border" Background="#909090"  CornerRadius="4,4,0,0" Margin="1 0 0 0" TextElement.FontSize="14" MinWidth="50">
                                    <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="12,2,12,2"/>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True" >
                                    <Setter Property="Background" TargetName="Border" Value="#909090" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True" >
                                    <Setter Property="Background" TargetName="Border" Value="#909090" />
                                    <Setter Property="Cursor" TargetName="Border" Value="Hand" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True" >
                                    <Setter Property="Background" TargetName="Border" Value="#2096F4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="ActionButton" TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                RecognizesAccessKey="True"
                                                Margin="5,0,5,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- DataGrid Row Style for Favorite Highlighting -->
            <Style x:Key="FavoriteRowStyle" TargetType="DataGridRow">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <!-- This trigger will be set programmatically when a row is favorited -->
                    <Trigger Property="Tag" Value="IsFavorite">
                        <Setter Property="Background" Value="#FFFACD"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#E3F2FD"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Context Menu Style -->
            <Style x:Key="GridContextMenuStyle" TargetType="ContextMenu">
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <!-- Context Menu Item Style -->
            <Style x:Key="GridContextMenuItemStyle" TargetType="MenuItem">
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <!-- PackIcon Style for Context Menu Icons -->
            <Style x:Key="ContextMenuIconStyle" TargetType="md:PackIcon">
                <Setter Property="Width" Value="16"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="Margin" Value="0"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid Background="{DynamicResource MaterialDesignPaper}">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="300" Name="ShellRow"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <Menu DockPanel.Dock="Top">
                    <MenuItem Header="_File" FontSize="12" VerticalAlignment="Center">
                        <MenuItem Header="_New" Icon="{md:PackIcon Kind=FilePlusOutline}" />
                        <MenuItem Header="_Open" Name="BtnMenuOpen" Icon="{md:PackIcon Kind=FolderOutline}"/>
                        <MenuItem Header="_Save" Name="BtnMenuSave" Icon="{md:PackIcon Kind=ContentSaveOutline}" />
                        <MenuItem Header="SaveAs" Name="BtnMenuSaveAs" Icon="{md:PackIcon Kind=ContentSaveMoveOutline}" />
                        <MenuItem Header="_Import" Name="BtnMenuImport"  Icon="{md:PackIcon Kind=FileImportOutline}" />
                        <MenuItem Header="_Preferences" Name="BtnMenuSettings" Icon="{md:PackIcon Kind=CogOutline}" />
                        <MenuItem Header="Toggle Shell" IsCheckable="True" Name="BtnMenuToggleSub" />
                        <Separator />
                        <MenuItem Header="_Exit" />
                    </MenuItem>
                    <MenuItem Header="_Edit" FontSize="12" VerticalAlignment="Center">
                        <MenuItem Header="_Add" Name="BtnMenuAdd" Icon="{md:PackIcon Kind=AddBox}"/>
                        <MenuItem Header="_Duplicate" Name="BtnMenuDuplicate" Icon="{md:PackIcon Kind=ContentCopy}"/>
                        <MenuItem Header="_Remove" Name="BtnMenuRemove" Icon="{md:PackIcon Kind=TrashCan}"/>
                        <MenuItem Header="_Undo Delete" Name="BtnMenuUndoDelete" Icon="{md:PackIcon Kind=DeleteRestore}"/>
                        <MenuItem Header="_Favorite" Name="BtnMenuFavorite" Icon="{md:PackIcon Kind=Star}"/>
                        <MenuItem Header="_Edit Mode" IsCheckable="True" Name="BtnMenuEdit" Icon="{md:PackIcon Kind=Edit}"/>
                    </MenuItem>
                    <MenuItem Header="_Run" FontSize="12" VerticalAlignment="Center">
                        <MenuItem Name="BtnMenuRunAttached" Header="Run (_Attached)" Icon="{md:PackIcon Kind=Play}"/>
                        <MenuItem Name="BtnMenuRunDetached" Header="Run (_Detached)" Icon="{md:PackIcon Kind=OpenInNew}"/>
                        <MenuItem Name="BtnMenuRunReopenLast" Header="Modify Last Command" Icon="{md:PackIcon Kind=FileDocumentEdit}"/>
                        <MenuItem Name="BtnMenuRunRerunLast" Header="Rerun Last Command" Icon="{md:PackIcon Kind=Refresh}"/>
                        <MenuItem Name="BtnMenuRunCopyToClipboard" Header="Copy to Clipboard" Icon="{md:PackIcon Kind=ClipboardMultipleOutline}"/>
                    </MenuItem>
                </Menu>
                <TextBox AcceptsReturn="True" />
            </DockPanel>

            <Grid Name="Main" Grid.Row="1">
                <TabControl Name="TabControl" Margin="1 0 0 0" HorizontalContentAlignment="Left" />
            </Grid>

            <GridSplitter Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Gray"/>

            <Grid Name="Shell" Grid.Row="3" Visibility="Visible" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                </Grid.RowDefinitions>
                <TabControl Grid.Row="0" Name="TabControlShell" HorizontalContentAlignment="Right" TabStripPlacement="Top" Margin="10,0,10,0" >
                    <TabItem Header="Shell">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5,0,5">
                                <Button Name="BtnPSAttachTab" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                                    <Button.LayoutTransform>
                                        <ScaleTransform  ScaleX="1.5" ScaleY="1.5"/>
                                    </Button.LayoutTransform>
                                    <md:PackIcon Kind="Import" Foreground="Green"/>
                                    <Button.ToolTip>
                                        <ToolTip>
                                            <TextBlock>Attach PS Window</TextBlock>
                                        </ToolTip>
                                    </Button.ToolTip>
                                </Button>
                                <Button Name="BtnPSDetachTab" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                                    <Button.LayoutTransform>
                                        <ScaleTransform  ScaleX="1.5" ScaleY="1.5"/>
                                    </Button.LayoutTransform>
                                    <md:PackIcon Kind="Export" Foreground="Red"/>
                                    <Button.ToolTip>
                                        <ToolTip>
                                            <TextBlock>Detach PS Tab</TextBlock>
                                        </ToolTip>
                                    </Button.ToolTip>
                                </Button>
                            </StackPanel>
                            <TabControl Name="PSTabControl" Grid.Row="0" HorizontalContentAlignment="Left" >
                                <TabItem Header="+" Name="PSAddTab"/>
                            </TabControl>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Log">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabControl Name="LogTabControl" Grid.Row="1" HorizontalContentAlignment="Left">
                                <TabItem Header="Debug" Grid.Row="1" HorizontalContentAlignment="Left" Visibility="Collapsed">
                                    <Grid ClipToBounds="True">
                                        <TextBox Name="LogBox" BorderThickness="0" Foreground="Black"  VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap" />
                                    </Grid>
                                </TabItem>
                                <TabItem Header="Command History" Grid.Row="1" HorizontalContentAlignment="Left">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <DataGrid Name="CommandHistoryGrid" Grid.Row="0"
                                                  AutoGenerateColumns="False"
                                                  IsReadOnly="True"
                                                  CanUserAddRows="False"
                                                  CanUserDeleteRows="False"
                                                  SelectionMode="Single"
                                                  HeadersVisibility="Column"
                                                  GridLinesVisibility="Horizontal"
                                                  AlternatingRowBackground="#F5F5F5"
                                                  RowHeight="30">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="150"/>
                                                <DataGridTextColumn Header="Command" Binding="{Binding CommandName}" Width="200"/>
                                                <DataGridTextColumn Header="Parameters" Binding="{Binding ParameterSummary}" Width="*"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
                                            <Button Name="BtnReopenHistoryCommand" Content="Reopen Command" Style="{StaticResource MaterialDesignRaisedButton}" TextElement.Foreground="White" Width="150" Height="30" Margin="5,0,5,0"/>
                                            <Button Name="BtnClearHistory" Content="Clear History" Style="{StaticResource MaterialDesignRaisedButton}" TextElement.Foreground="White" Width="150" Height="30" Margin="5,0,5,0"/>
                                        </StackPanel>
                                    </Grid>
                                </TabItem>
                                <TabItem Header="+" Name="LogAddTab"/>
                            </TabControl>
                        </Grid>
                    </TabItem>
                </TabControl>
                <Separator Grid.Row="1" />
            </Grid>

            <Grid Name="CommandControls" Grid.Row="4">
                <StackPanel Grid.Row="0" Margin="50 10 10 10" VerticalAlignment="Center" Orientation="Horizontal" Background="Transparent" HorizontalAlignment="Center">
                    <Button Name="BtnMainRun" Content="Run" IsDefault="True" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource MaterialDesignRaisedButton}" TextElement.Foreground="White" Width="250" Height="30"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <Separator Grid.Row="0" />
                <TextBlock Grid.Row="1" Name="StatusBox" Text="Ready" HorizontalAlignment="Right" Margin="0,0,10,0"/>
            </Grid>
        </Grid>

        <Rectangle Name="Overlay" Fill="Black" Opacity="0.5" Visibility="Collapsed" Panel.ZIndex="999"/>

        <!-- Loading Indicator -->
        <Grid Name="LoadingIndicator" Panel.ZIndex="1001" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border Background="White" BorderBrush="#2096F4" BorderThickness="2" CornerRadius="5" Padding="20">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="Loading" Width="32" Height="32" Foreground="#2096F4" Margin="0,0,10,0">
                        <md:PackIcon.RenderTransform>
                            <RotateTransform CenterX="16" CenterY="16"/>
                        </md:PackIcon.RenderTransform>
                        <md:PackIcon.Triggers>
                            <EventTrigger RoutedEvent="md:PackIcon.Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="(md:PackIcon.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </md:PackIcon.Triggers>
                    </md:PackIcon>
                    <TextBlock Name="LoadingText" Text="Loading command parameters..." FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Name="CommandDialog" Panel.ZIndex="1000" Background="White" Visibility="Hidden" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <Border BorderBrush="Black" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Height="25">
                        <TextBlock Name="BoxCommandName" HorizontalAlignment="Center" Margin="4 0 0 0" FontSize="20" FontWeight="Bold" />
                        <Button Name="BtnCommandClose" HorizontalAlignment="Right" IsEnabled="True" Margin="0 0 4 4" WindowChrome.IsHitTestVisibleInChrome="True" Style="{StaticResource ActionButton}" VerticalAlignment="Center" Height="25" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#9E9E9E">
                            <md:PackIcon Kind="Close" Height="20" Width="20"/>
                        </Button>
                    </Grid>

                    <Grid Name="CommandGrid" Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="100"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="250"/>
                        </Grid.ColumnDefinitions>
                        <!-- <Button Name="BtnCommonParameters" Content="Fold/Expand"/> -->
                    </Grid>

                    <Grid Grid.Row="2">
                        <Button Name="BtnCommandRun" Content="Run" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}" TextElement.Foreground="White" Width="150" Height="30" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Right" >
                            <Button Name="BtnCommandCopyToClipboard" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                            <Button.LayoutTransform>
                                <ScaleTransform   ScaleX="1.5" ScaleY="1.5"/>
                            </Button.LayoutTransform>
                                <md:PackIcon Kind="ClipboardMultipleOutline" Foreground="Black"/>
                                <Button.ToolTip>
                                    <ToolTip>
                                        <TextBlock>Copy Current Command to Clipboard</TextBlock>
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                            <Button Name="BtnCommandHelp" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource ActionButton}" >
                            <Button.LayoutTransform>
                                <ScaleTransform   ScaleX="1.5" ScaleY="1.5"/>
                            </Button.LayoutTransform>
                                <md:PackIcon Kind="Help" Foreground="Black"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid Name="SettingsDialog" Panel.ZIndex="1000" Background="White" Visibility="Hidden" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border BorderBrush="Black" BorderThickness="1">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Height="25">
                            <TextBlock Text="Settings" HorizontalAlignment="Center" Margin="4 0 0 0" FontSize="20" FontWeight="Bold" />
                            <Button Name="BtnCloseSettings" HorizontalAlignment="Right" IsEnabled="True" Margin="0 0 4 4" WindowChrome.IsHitTestVisibleInChrome="True" Style="{StaticResource ActionButton}" VerticalAlignment="Center" Height="25" Width="25" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#9E9E9E">
                                <md:PackIcon Kind="Close" Height="20" Width="20"/>
                            </Button>
                        </Grid>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="400">
                            <Grid Name="SettingsGrid">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="Default Shell:"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Name="TxtDefaultShell" Margin="5"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Default Shell Args:"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Name="TxtDefaultShellArgs" Margin="5"/>

                                <Label Grid.Row="2" Grid.Column="0" Content="Default Run Command Attached:"/>
                                <CheckBox Grid.Row="2" Grid.Column="1" Name="ChkRunCommandAttached" Margin="5" VerticalAlignment="Center"/>

                                <Label Grid.Row="3" Grid.Column="0" Content="Open Shell At Start:"/>
                                <CheckBox Grid.Row="3" Grid.Column="1" Name="ChkOpenShellAtStart" Margin="5" VerticalAlignment="Center"/>

                                <Label Grid.Row="4" Grid.Column="0" Content="Default Logs Path:"/>
                                <TextBox Grid.Row="4" Grid.Column="1" Name="TxtDefaultLogsPath" Margin="5"/>
                                <Button Grid.Row="4" Grid.Column="2" Name="BtnBrowseLogs" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                                    <Button.LayoutTransform>
                                        <ScaleTransform   ScaleX="1.5" ScaleY="1.5"/>
                                    </Button.LayoutTransform>
                                    <md:PackIcon Kind="FileFind" Foreground="Black"/>
                                    <Button.ToolTip>
                                        <ToolTip>
                                            <TextBlock>Change Path</TextBlock>
                                        </ToolTip>
                                    </Button.ToolTip>
                                </Button>

                                <Label Grid.Row="5" Grid.Column="0" Content="Settings Path:"/>
                                <TextBox Grid.Row="5" Grid.Column="1" Name="TxtSettingsPath" Margin="5"/>
                                <Button Grid.Row="5" Grid.Column="2" Name="BtnBrowseSettings" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                                    <Button.LayoutTransform>
                                        <ScaleTransform   ScaleX="1.5" ScaleY="1.5"/>
                                    </Button.LayoutTransform>
                                    <md:PackIcon Kind="FileFind" Foreground="Black"/>
                                    <Button.ToolTip>
                                        <ToolTip>
                                            <TextBlock>Change Path</TextBlock>
                                        </ToolTip>
                                    </Button.ToolTip>
                                </Button>

                                <Label Grid.Row="6" Grid.Column="0" Content="Favorites Path:"/>
                                <TextBox Grid.Row="6" Grid.Column="1" Name="TxtFavoritesPath" Margin="5"/>
                                <Button Grid.Row="6" Grid.Column="2" Name="BtnBrowseFavorites" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                                    <Button.LayoutTransform>
                                        <ScaleTransform   ScaleX="1.5" ScaleY="1.5"/>
                                    </Button.LayoutTransform>
                                    <md:PackIcon Kind="FileFind" Foreground="Black"/>
                                    <Button.ToolTip>
                                        <ToolTip>
                                            <TextBlock>Change Path</TextBlock>
                                        </ToolTip>
                                    </Button.ToolTip>
                                </Button>

                                <Label Grid.Row="7" Grid.Column="0" Content="Default Data File:"/>
                                <TextBox Grid.Row="7" Grid.Column="1" Name="TxtDefaultDataFile" Margin="5"/>
                                <Button Grid.Row="7" Grid.Column="2" Name="BtnBrowseDataFile" VerticalAlignment="Center" Style="{StaticResource ActionButton}">
                                    <Button.LayoutTransform>
                                        <ScaleTransform   ScaleX="1.5" ScaleY="1.5"/>
                                    </Button.LayoutTransform>
                                    <md:PackIcon Kind="FileFind" Foreground="Black"/>
                                    <Button.ToolTip>
                                        <ToolTip>
                                            <TextBlock>Change Path</TextBlock>
                                        </ToolTip>
                                    </Button.ToolTip>
                                </Button>

                                <Label Grid.Row="8" Grid.Column="0" Content="Command History Limit:"/>
                                <TextBox Grid.Row="8" Grid.Column="1" Name="TxtCommandHistoryLimit" Margin="5"/>

                                <Label Grid.Row="9" Grid.Column="0" Content="Show Debug Tab:"/>
                                <CheckBox Grid.Row="9" Grid.Column="1" Name="ChkShowDebugTab" Margin="5" VerticalAlignment="Center"/>
                            </Grid>
                        </ScrollViewer>

                        <Button Grid.Row="2" Name="BtnApplySettings" Content="Apply" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}" TextElement.Foreground="White" Width="150" Height="30" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                    </Grid>
                </Border>
            </Grid>
    </Grid>
</Window>